spring:
  profiles:
    active: druid
  redis:
    host: localhost
    port: 6379
    password: xxxxx
    database: 0
    lettuce:
      pool:
        max-active: 30
        min-idle: 10
        max-wait: -1
    timeout: 1000

cattle:
  upload:
    path: D:\临时文件\temp

server:
  port: 5000
logging:
  level:
    com.cattle: info
    com.cattle.mapper: debug
